# Code Agent Rules

Project: `<project-name>`

## Scope

This file defines rules for Code Agents working inside this product repository.

## Rules

- Read `CLAUDE.md` or the equivalent main project context file before changing code.
- Read relevant `knowledge/*.md` files before changing related behavior.
- Do not commit secrets, tokens, passwords, `.env` files, or credentials.
- Keep changes inside the explicitly requested scope.
- Do not rewrite unrelated files or perform style cleanup without approval.
