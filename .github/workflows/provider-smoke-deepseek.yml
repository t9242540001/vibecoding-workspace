name: Provider Smoke DeepSeek

on:
  workflow_dispatch:
    inputs:
      model:
        description: DeepSeek model to test
        required: true
        default: deepseek-v4-flash
        type: string
      base_url:
        description: DeepSeek OpenAI-compatible base URL
        required: true
        default: https://api.deepseek.com
        type: string

permissions:
  contents: read

jobs:
  deepseek-smoke:
    runs-on: ubuntu-latest

    steps:
      - name: Validate inputs
        env:
          MODEL: ${{ inputs.model }}
          BASE_URL: ${{ inputs.base_url }}
        run: |
          set -euo pipefail

          if [ "$BASE_URL" != "https://api.deepseek.com" ]; then
            echo "Invalid base_url. Expected https://api.deepseek.com."
            exit 1
          fi

          if [ -z "$MODEL" ]; then
            echo "Model input must not be empty."
            exit 1
          fi

          echo "Inputs validated."

      - name: Call DeepSeek chat completions
        env:
          MODEL: ${{ inputs.model }}
          BASE_URL: ${{ inputs.base_url }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: |
          set -euo pipefail

          if [ -z "$DEEPSEEK_API_KEY" ]; then
            echo "DEEPSEEK_API_KEY secret is not available."
            exit 1
          fi

          python - <<'PY' > /tmp/deepseek-payload.json
          import json
          import os

          payload = {
              "model": os.environ["MODEL"],
              "messages": [
                  {"role": "system", "content": "You are a smoke test responder."},
                  {"role": "user", "content": "Reply with exactly: OK"},
              ],
              "thinking": {"type": "disabled"},
              "temperature": 0,
              "max_tokens": 32,
          }
          print(json.dumps(payload))
          PY

          http_status="$(
            curl -sS \
              -o /tmp/deepseek-response.json \
              -w "%{http_code}" \
              -X POST "$BASE_URL/chat/completions" \
              -H "Authorization: Bearer $DEEPSEEK_API_KEY" \
              -H "Content-Type: application/json" \
              --data @/tmp/deepseek-payload.json
          )"

          echo "$http_status" > /tmp/deepseek-http-status.txt
          echo "HTTP status: $http_status"
          echo "Model: $MODEL"

          case "$http_status" in
            2*) ;;
            *)
              echo "DeepSeek request failed."
              if [ -s /tmp/deepseek-response.json ]; then
                echo "Response body:"
                cat /tmp/deepseek-response.json
              fi
              exit 1
              ;;
          esac

      - name: Parse DeepSeek response
        env:
          MODEL: ${{ inputs.model }}
        run: |
          set -euo pipefail

          python - <<'PY'
          import json
          import os
          import sys

          with open("/tmp/deepseek-response.json", "r", encoding="utf-8") as f:
              data = json.load(f)

          choice = data["choices"][0]
          message = choice.get("message", {})
          content = (message.get("content") or "").strip()
          reasoning_content = message.get("reasoning_content")
          finish_reason = choice.get("finish_reason")
          response_model = data.get("model")
          usage = data.get("usage")

          if not content:
              print("Parsed content is empty.")
              print(f"Top-level keys: {sorted(data.keys())}")
              print(f"Response model: {response_model}")
              print(f"Finish reason: {finish_reason}")
              print(f"Message keys: {sorted(message.keys())}")
              print(f"Reasoning content present: {reasoning_content is not None}")
              if usage is not None:
                  print(f"Usage: {json.dumps(usage, sort_keys=True)}")
              sys.exit(1)

          with open("/tmp/deepseek-http-status.txt", "r", encoding="utf-8") as f:
              http_status = f.read().strip()

          print(f"HTTP status: {http_status}")
          print(f"Requested model: {os.environ['MODEL']}")
          print(f"Response model: {response_model}")
          print(f"Finish reason: {finish_reason}")
          print(f"Parsed content: {content}")
          if usage is not None:
              print(f"Usage: {json.dumps(usage, sort_keys=True)}")
          PY

      - name: Upload smoke response
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: deepseek-smoke-response
          path: /tmp/deepseek-response.json
          if-no-files-found: ignore
          retention-days: 3
